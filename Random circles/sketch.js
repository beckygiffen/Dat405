//RANDOMCIRCLESBABES
let size = random(500);

function setup() {
  createCanvas(500, 500);
  frameRate(20);
}

function draw() {
  //Run the nested loop for x, and y

      //Draw the background shapes
      fill(random(100), random(0), random(255), random(150))
      noStroke();

      ellipse(random(500), random(500), random(500));


    }
